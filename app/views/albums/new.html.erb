<% content_for :title, "Create a new Album" %>

<h1>Create Album</h1>

<%= form_for @album do |f| %>
  <%= f.text_field :name %>
  <%= f.label :name %>
  <br>

  <% @photos.each do |p| %>
    <%= label_tag dom_id(p), (image_tag processed?(p), class: 'checkbox-photo') %>
    <%= check_box_tag "album[photo_ids][]", p.id, @album.photo_ids.include?(p.id), id: dom_id(p), class: 'checkbox' %>
  <% end %>

  <%= f.submit %>
<% end %>