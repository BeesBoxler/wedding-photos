<p id="notice"><%= notice %></p>
<table>
  
  <tr>
    <td>Uploaded by <%= @photo.user.name %></td>
    <td><%= image_tag @photo.image.url(:full) %></td>
  </tr>

</table>

<%= link_to 'Back', photos_path %>