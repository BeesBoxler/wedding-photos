<h1><a href="#" id="show_photos" class="active">All photos</a> &bull; <a href="#" id="show_albums">All albums</a></h1>
<div id="container">
  <section id="photos">
    <%= will_paginate @photos, param_name: 'photo_page' %>
    <% @photos.each do |photo| %>
      <div class="photo single">
        <%= link_to image_tag( processed?(photo) ),
                    photo.image.url,
                    title: photo.title,
                    rel: "wedding",
                    class: "fancybox"  %>
        <% if current_user?(photo.user) %>
          <%= link_to raw('<i class="fa fa-pencil"></i>'),
                      photo,
                      title: "Edit Photo",
                      class: "edit" %>
          <%= link_to raw('<i class="fa fa-trash-o"></i>'),
                      photo,
                      title: "Delete Photo",
                      method: :delete, 
                      data: { confirm: 'Are you sure?' },
                      class: "delete" %>
        <% end %>
        <%= link_to raw('<i class="fa fa-link"></i>'),
                    photo,
                    title: "Share Photo Link",
                    class: "share" %>
        <p>Uploaded by <%= photo.user.name %></p>
      </div>
    <% end %>

    <%= will_paginate @photos, param_name: 'photo_page' %>

  </section>
  <section id="albums">
    <%= will_paginate @albums, param_name: 'album_page' %>

    <% @albums.each do |a| %>
      <div class="photo album">
        <span class="title"><%= a.name %></span>
        <%= link_to image_tag( processed?(a.photo) ), album_path(a) %>
      </div>
    <% end %>
    <%= will_paginate @albums, param_name: 'album_page' %>

  </section>
</div>
<div id="link_share">
  <input name="clipboard-text" id="clipboard-text" class="link_share" type="text" value="">
  <button id="target-to-copy" data-clipboard-target="clipboard-text">Click To Copy</button>
</div>